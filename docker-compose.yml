version: '3.4'
  
services:
  db:
    image: mcr.microsoft.com/mssql/server:2017-latest
    environment:
      - SA_PASSWORD=Az!05120512
      - ACCEPT_EULA=Y
    ports:
      - "5434:1433"

  importcsv:
      depends_on:
        - db
      image: importcsv
      build:
        context: .
        dockerfile: Dockerfile
      ports:
        - 5000:5000